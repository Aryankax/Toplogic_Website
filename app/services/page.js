'use client';
import React, { useState, useRef, useEffect } from "react";
import Navbar from "@/components/nav";

const Home = () => {

    return (
        <>
            <Navbar></Navbar>
            <h1 className="text-center text-white text-4xl mt-5 font-Poppins font-semibold uppercase">Services</h1>
        </>
    );
};

export default Home;
